<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hardgusol.sol">

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Blue_Theme"
        tools:ignore="AllowBackup">
        <activity
            android:name="org.farmer.wordle.ShowALLMatchesActivity"
            android:exported="true" />
        <activity
            android:name="org.farmer.wordle.TestActivity"
            android:exported="true"></activity>
        <activity
            android:name="org.farmer.wordle.HelpHtmlActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="org.farmer.wordle.MainGActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>